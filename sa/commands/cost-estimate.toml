# Gemini CLI Custom Commands for a Solution Architect
# In: ~/.gemini/extensions/sa/commands/cost-estimate.toml
# Invoked via: /cost-estimate

name = "cost-estimate"
tool = "web_fetch"

[args]
prompt = "Use the GCP pricing pages for {instance} and estimate the monthly cost."
