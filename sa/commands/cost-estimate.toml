name = "cost-estimate"
prompt = "Estimate the monthly cost for a {instance_type} on {cloud_provider}"
tool = "web_fetch"

# Example: gemini cost-estimate --instance_type "n2-standard-4" --cloud_provider "GCP"

[args]
prompt = "Find the official pricing calculator for {cloud_provider} and estimate the monthly cost for a {instance_type} instance."
